{
    "name": "cli-in-simple-typescript-project",
    "version": "0.0.1",
    "description": "A test harness for image-classifier-ts (as cli) to test consuming the package.",
    "main": "dist/",
    "scripts": {
        "build": "yarn clean && tsc --module commonjs && yarn lint",
        "clean": "rm -rf dist/",
        "lint": "yarn tslint --project tsconfig.json --format msbuild"
    },
    "author": "Sean Ryan <mr.sean.ryan(at)gmail.com>",
    "license": "MIT",
    "dependencies": {
        "image-classifier-ts": "^2.0.9"
    },
    "private": true,
    "devDependencies": {
        "@types/cli-color": "^0.3.29",
        "@types/yargs": "^12.0.1",
        "cli-color": "^1.4.0",
        "ts-node": "^7.0.1",
        "tslint": "^5.11.0",
        "typescript": "^3.2.2",
        "yargs": "^12.0.5"
    }
}
